import { AppShellClient } from "@/components/MapCanvasClient";

export default function HomePage() {
  return (
    <main className="h-screen w-screen overflow-hidden bg-[#0c1421]">
      <AppShellClient />
    </main>
  );
}
